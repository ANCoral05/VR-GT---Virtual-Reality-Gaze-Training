using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Navigation_MainScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //For measuring user's orientation, use relative position of headset to controllers (as well as headset orientation and controller orientation) - this allows to turn the head without the software falsely registering a change in body orientation.
    }
}
